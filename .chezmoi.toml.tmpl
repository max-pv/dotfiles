[data]
    name = {{ promptString "Git name" | quote }}
    email = {{ promptString "Git email" | quote }}
    ssh_git_hosts = {{ promptString "SSH git hosts (comma-separated, e.g. git.example.com)" | splitList "," | toJson }}
    goprivate = {{ promptString "GOPRIVATE (leave empty if none)" | quote }}
